#pragma once

namespace abcd{
    template<typename T>
    struct index_t{};

//    template<typename T>
//    constexpr index_t<T> idx{};

    template<typename T>
    struct capability_t{};

//    template<typename T>
//    constexpr capability_t<T> capability{};
}